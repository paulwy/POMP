# HR与用户账号一体化管理设计方案

## 设计原则

1. **单一数据源原则**：员工档案与系统账号使用同一表（`users`），避免数据冗余和不一致
2. **权限分离原则**：通过权限体系区分管理员和普通员工
3. **流程标准化原则**：员工入职、创建账号、设置权限流程标准化
4. **数据完整性原则**：确保必填字段完整性，支持灵活扩展

## 系统架构

### 数据库表结构

当前 `users` 表已包含：
- **基础账号字段**：username, password_hash, email, is_active, is_superuser
- **员工信息字段**：name, phone, employee_no, hire_date, position_id, status

### 角色与权限体系

| 角色类型 | is_superuser | 功能权限 |
|---------|-------------|---------|
| 系统管理员 | true | 全部功能 |
| HR管理员 | false（需通过权限体系） | HR管理、员工管理 |
| 普通员工 | false | 个人考勤、请假申请 |

## HR创建员工标准流程

```
1. 录入员工基本信息
   ├─ 姓名（必填）
   ├─ 邮箱（必填，唯一）
   ├─ 工号（必填，唯一）
   ├─ 手机号（可选）
   ├─ 入职日期（必填）
   └─ 职位（可选）

2. 系统自动生成账号信息
   ├─ username：使用工号（企业通行做法）
   ├─ password：初始默认密码（如：工号@123）
   ├─ 状态：默认为 active
   └─ 角色：普通员工

3. 通知与首次登录
   ├─ 发送账号信息到员工邮箱
   └─ 首次登录强制修改密码
```

## 实现方案

### 前端修改（HrManagement）

**创建员工表单增加字段：**
```typescript
interface CreateEmployeeRequest {
  name: string;          // 员工姓名
  email: string;         // 邮箱（作为联系方式）
  phone?: string;        // 手机号
  employee_no: string;   // 工号（自动生成username）
  hire_date: string;     // 入职日期
  position_id?: string;  // 职位ID
  department_id?: string;// 部门ID（如需）
}
```

### 后端修改（hr.rs）

**创建员工逻辑：**
```rust
// 1. username 使用工号
// 2. 生成初始密码哈希
// 3. 状态设为 active
// 4. is_superuser 设为 false
```

## 默认密码策略

**方案A：工号+固定后缀（推荐）**
- 默认密码：`{employee_no}@123`
- 优点：员工易记忆，HR易告知
- 示例：工号 E001 的密码为 E001@123

**方案B：随机生成+邮件通知**
- 生成随机密码并发送邮箱
- 优点：更安全
- 缺点：依赖邮件服务

**推荐采用方案A**，更符合企业HR操作习惯。

## Username生成策略

| 策略 | 说明 | 优点 | 缺点 |
|-----|------|------|------|
| 工号 | username = employee_no | 企业通行，易管理 | 需要确保工号唯一性 |
| 邮箱前缀 | username = email@前部分 | 易记忆 | 可能重复 |
| 姓名拼音 | username = pinyin(name) | 易读 | 同姓名重复问题 |

**推荐使用工号**，这是中国企业最通行的做法。

## 数据库唯一性约束

```sql
-- 已存在：
UNIQUE(username)
UNIQUE(email)
UNIQUE(employee_no)

-- 确保：
-- 1. 用户名不能重复
-- 2. 邮箱不能重复  
-- 3. 工号不能重复
```

## 后续优化方向

1. **首次登录强制修改密码**：增加字段标记是否首次登录
2. **密码策略**：设置密码强度要求、过期时间
3. **账号激活流程**：通过邮件链接激活
4. **批量导入**：支持Excel批量导入员工
5. **账号冻结/离职处理**：员工离职时的账号处理流程
